config:
  schema:
    introspectionEndpoint: https://gapi-api-example.herokuapp.com/graphql
    introspectionOutputFolder: ./src/app/core/api-introspection

commands:
  typings:
    generate: gapi schema introspect --collect-documents --collect-types